import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DevRel Agent | AI-Powered GitHub Issue Resolver",
  description:
    "Autonomous agent that reads GitHub issues, analyzes intent with GPT-4o-mini, and opens ready-to-merge Pull Requests.",
  openGraph: {
    title: "DevRel Agent | AI-Powered GitHub Issue Resolver",
    description:
      "Stop triaging manually. DevRel Agent reads issues, categorizes them, and drafts PRs autonomously.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
